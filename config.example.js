// Configuration — copy this file to config.js and fill in your base URLs.
// config.js is excluded from version control (.gitignore).
const URLS = {
  dev: "https://your-dev-url.vercel.app/",
  staging: "https://your-staging-url.vercel.app/",
  custom: ""
};